@page "/Candidates"

@inject AppData AppData

<PageTitle>Where do they stand?</PageTitle>

<p>Access to Safe Legal Abortion is a Human Right.  This is codified by the United Nations in the UN Convention on Civil and Political Rights via General Comment No. 36 point 8 (2019).</p>

<p>150 Republican House Members have co-sponsored the Life At Conception Act of 2021 (HR 1011), which would revoke that right in US law, banning the right to Abortion nationwide, without exceptions.</p>

<p>These are the PACs that have contributed money for the 2021-2022 election cycle for those candidates, ordered by the amount donated, as recorded by OpenSecrets.org.</p>

<p>Data from the top 100 contributors to each candidate from OpenSecrets.org as of 5/12/2022.  Individual contributions not included.</p>

@foreach(var candidate in AppData.candidateDict.OrderByDescending(x => x.Value.Sum(y => y.Pacs)))
{
    <h1><a href="Candidate/@candidate.Key">@AppData.candidateMap[@candidate.Key].name</a></h1>
    <p>$@candidate.Value.Sum(y => y.Pacs) donated from @candidate.Value.Where(x => x.Pacs > 0).Count() PACs.</p>
}